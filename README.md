# appsync-handle-files

## Requirements

* AWS account and CLI configured
* Terraform

## Deploy

* ```terraform init```
* ```terraform apply````

## Usage

Open the page at the output URL, log in with users, upload images.

## Cleanup

* ```terraform destroy```
